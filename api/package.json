{
  "name": "api",
  "version": "1.0.0",
  "description": "Proxy server for MyStream app.",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-cache": "^5.1.2",
    "node-fetch": "^2.7.0"
  }
}
